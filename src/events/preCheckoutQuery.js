import bot from "../app.js"

const preCheckoutQuery = async (evt) => {
    try {
        console.log(evt)

        // bot.answerPreCheckoutQuery(evt.id)
    } catch (error) {
        
    }
}

export default preCheckoutQuery